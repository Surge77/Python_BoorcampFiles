"""
WAP to print the index of values in a list

Tip: The index must be an integer,if you specify a non integer number as the index then typeerror will be generated
"""

num_list = [1,2,3,4,5]

for i in range(len(num_list)):
    print("index : ",i)